# Phase-B capture pack — INDEX

Generated by `pnpm capture` (viewport 360×740, deviceScaleFactor 2, reduced-motion).
Each frame is the REAL play UI, driven to its state by replaying the committed fixture
log through the dev `window.__replay(seed, actions)` hook — deterministic and rerunnable.

The mid-drag frames (`DRAG_*`) use REAL pointer input — mouse down on the card, move past
the 8 px tap slop, hold over the target so the glow paints, shoot, then release in dead
space (the opponent row carries no drop zone) so the game state is unchanged. The C4
"nothing to pay" beat (`S4`) and the NAHI-chain resolution beat (`S2`) are held by the
dev capture-freeze (`__replay` pauses the bot timer / auto-draw / auto-resolve) so the
transient beat can be shot without advancing the game.

## Recorded fixture states

| Image | State it shows | Replay id (seed) | Spec rows |
|-------|----------------|------------------|-----------|
| `S9_adla_badli.png` | ADLA-BADLI is legal (two-dimension pick) | `S9_adla_badli` (seed 1) | B16/B18 |
| `S3_pay_from_bank.png` | C1/C3: charged, and my bank covers it without breaking a set | `S3_pay_from_bank` (seed 1) | C1/C3 |
| `S6_makaan.png` | B19: I can legally play MAKAAN on my complete set | `S6_makaan` (seed 1) | B19 |
| `S11_declare_win.png` | A11: I reach a declarable SAUDA win | `S11_declare_win` (seed 2) | A11 |
| `S1_nahi_hold.png` | D1: a bot attacks me and I hold NAHI CHALEGA | `S1_nahi_hold` (seed 3) | D1 |
| `S2_nahi_chain.png` | D3: a NAHI chain — attack, counter, counter | `S2_nahi_chain` (seed 3) | D3 |
| `S7_wild_lagaan.png` | B14: I can play a wild LAGAAN (colour + target) | `S7_wild_lagaan` (seed 4) | B14 |
| `S9_kabza.png` | KABZA is legal against a complete opponent set | `S9_kabza` (seed 4) | B13 |
| `S8_dugna_x2.png` | B15: I can attach one DUGNA to a LAGAAN (x2) | `S8_dugna_x2` (seed 5) | B15 |
| `S6_haveli.png` | B19: I can legally play HAVELI on a set that has a MAKAAN | `S6_haveli` (seed 7) | B19 |
| `S4_zero_payable.png` | C4: charged with an empty table — zero payable | `S4_zero_payable` (seed 23) | C4 |
| `S8_dugna_x4.png` | B15: I can attach two DUGNA to a LAGAAN (x4) | `S8_dugna_x4` (seed 33) | B15 |
| `S5_discard_mode.png` | A8/A9: I end a turn over 7 cards — discard mode | `S5_discard_mode` (seed 1) | A8/A9 |
| `S10_eleven_cards.png` | Hand-fan stress: I hold 11 cards at once | `S10_eleven_cards` (seed 2) | A2 |

## Composite frames (report flag 5 — the mid-drag / advisor / dim states)

| Image | Frame it shows | Replay base | Spec rows |
|-------|----------------|-------------|-----------|
| `REST_my_turn.png` | Rest state on my own turn — hand fan, bank, groups, End-turn column; no overlay. | `S9_kabza` | A2 · A10 L2 |
| `DRAG_money_bank_hot.png` | A money card mid-drag held over the bank — the bank zone glows HOT. | `S9_adla_badli` | A10 L3/L5 · A4 |
| `DRAG_wild_two_groups.png` | A dual wildcard (mumbai|newDelhi) mid-drag held over neutral felt — BOTH its owned set groups glow (soft gold ring), the bank does NOT glow (no wildcard is bankable — canon). | seed 185 · 23-step recipe | A4 · §6.2 canon · L5 |
| `DRAG_placed_wild_rearrange.png` | A placed wildcard mid-rearrange — its "◈" token dragged out; the legal destination groups glow. | `S9_adla_badli` | B8 |
| `BOT_turn_dim.png` | A bot turn asleep — I end my turn, control passes to a bot, my area dims (off-turn sleep). | `S9_kabza` | A2 dimSleep · I1 |
| `MUNSHI_open.png` | The Munshi advisor open — the read-only "Munshi ki Salah" card over a slept board (one live surface). | `S9_kabza` | M4b H-rows · L2 |

### Composite requirements also satisfied by a fixture image

- **the payment sheet pre-filled** → `S3_pay_from_bank.png` (same state; no separate frame needed).
- **discard mode** → `S5_discard_mode.png` (same state; no separate frame needed).
- **the 11-card hand fan** → `S10_eleven_cards.png` (same state; no separate frame needed).

## NOT CAPTURED

None — every state and composite rendered and was shot cleanly.

## Scroll-guard / console

No console warnings during capture — in particular the `#/autostart` scroll guard stayed
silent, so no state scrolled the play surface (A2).

---

_Rerun:_ `pnpm capture` (from the repo root). Viewport is a parameter:
`pnpm capture --viewport=1080x1920` for M5 store screenshots.
